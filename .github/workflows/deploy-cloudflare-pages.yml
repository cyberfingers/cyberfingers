name: Deploy Cyberfingers to Cloudflare Pages

on:
  push:
    branches:
      - main
      - codex/cloudflare-rebuild
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cloudflare-pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          # Cloudflare Pages treats the configured production branch (main) as
          # production. Deploy this migration branch to that target until the
          # rebuilt site is merged, so custom domains receive the same release.
          command: pages deploy public --project-name=cyberfingers --branch=main
