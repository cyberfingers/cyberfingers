{
  "name": "cyberfingers-site",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=cyberfingers",
    "check": "wrangler pages functions build --outdir=.wrangler/tmp/functions"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
